name: second

on: workflow_dispatch

jobs:
  deployment:
    runs-on: ubuntu-latest

    steps:
      - name: printing second session
        run: echo "Devops second message"

      - name: github actions actions
        uses: actions/checkout@v7


