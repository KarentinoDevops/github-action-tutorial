name: sunday

on: 
  push:
  pull_request:

jobs:
  on pull and push:
    runs-on: ubuntu-latest

    steps:
    - name: first step
      run: echo "Hello, Github Actions!"

    - name: second step
      run: echo "I am happy today!"


  